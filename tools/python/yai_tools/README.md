# yai_tools (Canonical In yai-infra)

This package is the canonical governance/delivery tooling implementation for the YAI ecosystem.

Consumer repositories may keep mirrored copies during migration for CI compatibility,
but all functional changes must be authored in `yai-infra` first.
