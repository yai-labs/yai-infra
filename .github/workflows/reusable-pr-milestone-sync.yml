name: reusable-pr-milestone-sync

on:
  workflow_call:
    inputs:
      org:
        description: Organization/owner context for governance suite.
        required: false
        type: string
        default: "yai-labs"
      project_number:
        description: Project number context for governance suite.
        required: false
        type: number
        default: 0
      repo_scope:
        description: Repository scope selector (reserved for future use).
        required: false
        type: string
        default: ""
      dry_run:
        description: If true, report planned milestone sync without applying.
        required: false
        type: boolean
        default: false
    secrets:
      project_token:
        required: false

permissions:
  contents: read
  issues: read
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync PR milestone from linked issue
        uses: actions/github-script@v7
        with:
          script: |
            const dryRun = "${{ inputs.dry_run }}" === "true";
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const pr = context.payload.pull_request;
            const body = pr.body || "";

            const issueRefs = new Set();
            const closeRe = /\b(?:close[sd]?|fix(?:e[sd])?|resolve[sd]?)\s+(?:#(\d+)|https:\/\/github\.com\/[^/\s]+\/[^/\s]+\/issues\/(\d+))/gi;
            let m;
            while ((m = closeRe.exec(body)) !== null) {
              const n = m[1] || m[2];
              if (n) issueRefs.add(Number(n));
            }

            if (issueRefs.size === 0) {
              core.info("No closing issue reference found in PR body; skipping milestone sync.");
              return;
            }

            const issueNumber = [...issueRefs][0];
            core.info(`Using linked issue #${issueNumber} for milestone sync`);

            const { data: issue } = await github.rest.issues.get({
              owner,
              repo,
              issue_number: issueNumber,
            });

            if (!issue.milestone) {
              core.info(`Issue #${issueNumber} has no milestone; skipping milestone sync.`);
              return;
            }

            const targetMilestone = issue.milestone.number;
            const currentMilestone = pr.milestone ? pr.milestone.number : null;
            if (currentMilestone === targetMilestone) {
              core.info(`PR milestone already aligned to '${issue.milestone.title}'.`);
            } else {
              if (dryRun) {
                core.info(`dry_run=true: would set PR milestone to '${issue.milestone.title}' from issue #${issueNumber}.`);
                return;
              }
              await github.rest.issues.update({
                owner,
                repo,
                issue_number: pr.number,
                milestone: targetMilestone,
              });
              core.info(`PR milestone set to '${issue.milestone.title}' from issue #${issueNumber}.`);
            }
